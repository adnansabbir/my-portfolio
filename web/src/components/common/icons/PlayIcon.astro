---
import IconBase from './IconBase.astro';

interface Props {
	color?: string;
	size?: number;
	class?: string;
}

const { color, size, class: className } = Astro.props;
---

<IconBase color={color} size={size} class={className}>
	<path d="M6 3v18l15-9z" fill="currentColor" stroke="none"></path>
</IconBase>

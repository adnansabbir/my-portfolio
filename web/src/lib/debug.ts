// Not a secret - it sits in every page's source. Never gate anything sensitive on it.
export const DEBUG_PARAM = 'lantern';

export const DEBUG_STORAGE_KEY = 'debug-mode';
export const DEBUG_ON_VALUES = ['', 'true', '1', 'on', 'yes'];

SANITY_PROJECT_ID=
SANITY_DATASET=

# Exposed to the client bundle (PUBLIC_ prefix required by Astro/Vite) since
# gtag.js runs in the browser, unlike the Sanity vars above which are only
# read at build time.
PUBLIC_GA_MEASUREMENT_ID=
